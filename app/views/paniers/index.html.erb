<% content_for :meta_title, "#{DEFAULT_META["meta_product_name"]} - Le panier de la semaine" %>

<div class="wrapper panier-wrapper">

  <h1>Le panier de la semaine</h1>

  <% if user_signed_in? %>
    <%= link_to "Créer un panier", new_panier_path, class:"btn btn-primary btn-create" %>
  <% end %>
</div>

<div class="container text-center">

  <p>Les paniers sont à commander par téléphone avant le mercredi soir, et à venir chercher du jeudi à 18h45 au samedi soir.</p>
  <p>Prix : 12,50€</p>
    <% if Panier.first == nil %>
      <p>Aucun panier n'est prévu pour le moment.</p>
    <% end %>

  <% @paniers.each do |panier| %>
    <div class="panier-card">
      <h4><%= panier.title %></h4>
      <% if user_signed_in? %>
        <div class="row">
          <div class="col">
            <%= link_to "Modifier le panier", edit_panier_path(panier), class:"btn btn-success" %>
          </div>
          <div class="col">
            <%= button_to "Supprimer le panier", panier, :method => :delete, data: { confirm: 'Es-tu sûr(e) de vouloir supprimer ce panier ?' }, class:"btn btn-primary" %>
          </div>
        </div>
      <% end %>
      <p><%= panier.ingredients %></p>
    </div>
    <hr>
  <% end %>

</div>

<div class="container">
  <br>
  <%= link_to "Retour à l'accueil", root_path %>
</div>
